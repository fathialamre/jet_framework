name: jet_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  jet_framework:
    path: packages/jet_framework

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed: ^2.5.8
  retrofit_generator:

flutter:

  uses-material-design: true
  assets:
    - .env
